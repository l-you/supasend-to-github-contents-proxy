version: "3"

env:
  GOCACHE: /tmp/go-cache
  GOMODCACHE: /tmp/go-modcache
  GOLANGCI_LINT_CACHE: /tmp/golangci-lint-cache

tasks:
  go:fmt:
    desc: Format Go sources
    cmds:
      - |
        set -euo pipefail
        files="$(find . -type f -name '*.go' -not -path './vendor/*')"
        if [ -n "$files" ]; then
          gofmt -w $files
        fi

  go:lint:
    desc: Run golangci-lint for all Go packages
    cmds:
      - golangci-lint run ./...

  go:deadcode:
    desc: Run deadcode analysis for all packages
    preconditions:
      - sh: test -x "$(go env GOPATH)/bin/deadcode"
        msg: "deadcode is not installed. Install with: go install golang.org/x/tools/cmd/deadcode@latest"
    cmds:
      - |
        deadcode_bin="$(go env GOPATH)/bin/deadcode"
        output="$(HOME=/tmp XDG_CACHE_HOME=/tmp "$deadcode_bin" -test ./...)"
        if [ -n "$output" ]; then
          echo "$output"
          echo
          echo "deadcode validation failed: remove unreachable functions or justify exclusions."
          exit 1
        fi

  go:test:
    desc: Run Go tests for all packages
    cmds:
      - go test ./...

  go:e2e:memory:
    desc: Run opt-in real-server memory e2e test
    cmds:
      - go test -tags=e2e ./e2e -run TestServerMemoryCaptureE2E -count=1 -v

  gen:
    desc: Run all code generation
    cmds:
      - task: gen:check

  gen:check:
    desc: Check generated outputs
    cmds:
      - task: gen:code-diff

  gen:code-diff:
    desc: CI fallback generation diff check
    cmds:
      - git diff --exit-code

  fix:
    desc: Run auto-fixes
    cmds:
      - task: go:fmt

  validate:
    desc: Run project validation checks
    cmds:
      - task: go:lint
      - task: go:deadcode

  test:
    desc: Run test suite
    deps:
      - validate
    cmds:
      - task: go:test
